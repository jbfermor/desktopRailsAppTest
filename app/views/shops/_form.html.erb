<%= form_with(model: shop, url: customer_shops_path, method: :post) do |form| %>
  <% if shop.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(shop.errors.count, "error") %> prohibited this shop from being saved:</h2>

      <ul>
        <% shop.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.label :retailer_id, style: "display: block" %>
    <%= form.collection_select :retailer_id, @retailers, :id, :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
