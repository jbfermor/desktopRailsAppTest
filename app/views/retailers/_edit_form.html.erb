<%= form_with(model: retailer, method: :patch) do |form| %>
  <% if retailer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(retailer.errors.count, "error") %> prohibited this retailer from being saved:</h2>

      <ul>
        <% retailer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="m-2">
    <%= form.label :name, class: "form-label" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div class="m-2">
    <%= form.label :email, class: "form-label" %>
    <%= form.text_field :email, class: "form-control" %>
  </div>

  <div class="m-2">
    <%= form.label :customer_id, class: "form-label" %>
    <%= form.collection_select :customer_id, @customers, :id, :name, class: "form-select" %>
  </div>

  <div class="m-2">
    <%= form.submit class: "btn btn-success" %>
  </div>
<% end %>
